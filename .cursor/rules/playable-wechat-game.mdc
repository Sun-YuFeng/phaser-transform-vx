---
description: PlayableMaker 微信小游戏移植规范（phaser3.90.0 模板、wx 补丁、bundle 构建）
globs: src/wx/**,phaser3.90.0/**,vite/config.wx.mjs,scripts/copy-wx-assets.mjs,scripts/extract-runtime.mjs
alwaysApply: false
---

# PlayableMaker 微信小游戏

## 架构

- 微信入口：`phaser3.90.0/game.js`（require adapter → require bundle）
- 源码入口：`src/wx/main.js` → `npm run build:wx` → `phaser3.90.0/js/playable/bundle.js`
- 补丁文件：`src/wx/phaser-wx-patch.js`（改这里，不要改 bundle）
- 运行时：`src/playable/runtime.js`（从 output.html 提取，尽量少改）

## 必须遵守

```javascript
// ✅ 本地资源加载：patch File.prototype.load，不是 XHRLoader
// ✅ 图片完成：loader.nextFile(file, true)
// ✅ canvas：只读 window.canvas，禁止赋值 GameGlobal.canvas
// ✅ 微信 Phaser：WEBGL + canvas + parent: canvas
// ✅ 模块：CJS bundle，文件名 bundle.js
// ❌ 不要手动编辑 bundle.js
// ❌ 不要在 phaser3.90.0 加 demo 脚本或重复 assets
```

## 构建

```bash
npm run build:wx
```

资源由 `copy-wx-assets.mjs` 同步到 `{wx}/assets/`（仅此一份）；`vite/config.wx.mjs` 设 `publicDir: false`，避免 Vite 再拷 `public/` 到 `js/playable/`。自动跳过 mp4。

`vite/config.wx.mjs` 必须降级到 ES5 输出（微信不支持 runtime 里的 `??` / `?.`）：

- `build.target: 'es2015'`
- `esbuild.supported`: 关闭 `nullish-coalescing`、`optional-chain`
- `minify: 'terser'`，`terserOptions.format.ecma: 5`

禁止 `minify: false` 且无 target——会导致预览/上传报 `invalid file: bundle.js … Unexpected token ?`。

## 排错顺序

1. 预览/上传报 `Unexpected token ?` → 检查 `bundle.js` 是否含 `??`，重跑 `build:wx` 并确认 `config.wx.mjs` 上述配置
2. `wxPerf.now is not a function` / `getPerformance() is not implemented` → 确认 `js/libs/weapp-adapter.js` 已对 `wx.getPerformance()` 做 fallback（模板 `phaser3.90.0/js/libs/`）
3. 画面拉伸 / `Run game: w:undefined h:undefined` → 勿用 `wx.getWindowInfo()` 三元判断；用 `GameGlobal.wxGetWindowInfo()` / `wxSetupCanvas()`（`getSystemInfoSync` 兜底）
4. 看第一个 TypeError，不是 `.catch` 里的 `Cannot set def-template`
5. 确认 `!Load completted` 和 `DATA LOADED, Start Game` 是否出现
6. 查 `[wx]` 前缀的 readFile 错误

## 用户确认可交付

用户说「可以了」→ 运行 `npm run finish:game`（归档 sources/current → history，清理 public）。

详细流程见 `.cursor/skills/playable-wechat-game/SKILL.md`。
